{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "allOf": [
    {
      "$ref": "baseCircRequestDTO.json"
    }
  ],
  "description": "D2IR Transfer request",
  "type": "object",
  "properties": {
    "newItemId": {
      "description": "FOLIO inventory item HRID of the replacement item. The current itemId is validated against the stored value, then replaced with this newItemId.",
      "type": "string",
      "pattern": "[a-z,0-9]{1,32}"
    }
  },
  "additionalProperties": false,
  "required": [
    "newItemId"
  ]
}